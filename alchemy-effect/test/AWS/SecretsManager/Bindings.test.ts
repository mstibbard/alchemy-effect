import { describe, test } from "vitest";

describe("SecretsManager Bindings", () => {
  describe("DescribeSecret", () => {
    test.skip("describes a bound secret", () => {});
  });

  describe("GetSecretValue", () => {
    test.skip("reads a bound secret value", () => {});
  });

  describe("GetRandomPassword", () => {
    test.skip("generates a random password", () => {});
  });

  describe("ListSecrets", () => {
    test.skip("lists secrets in the account", () => {});
  });

  describe("PutSecretValue", () => {
    test.skip("writes a new bound secret version", () => {});
  });
});
