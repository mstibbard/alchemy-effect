{
  "name": "stacks",
  "version": "0.0.0",
  "private": true,
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/alchemy-run/alchemy-effect"
  },
  "type": "module",
  "scripts": {
    "download:env": "doppler secrets download -c dev --project alchemy-v2 --no-file --format env > .env",
    "login": "alchemy login ./github.ts --profile admin",
    "deploy": "alchemy deploy ./github.ts --profile admin",
    "destroy": "alchemy destroy ./github.ts --profile admin"
  },
  "dependencies": {
    "alchemy": "workspace:*",
    "effect": "catalog:"
  }
}